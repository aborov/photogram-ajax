document.querySelectorAll('.follow-container[data-user-id="<%= @recipient.id %>"]').forEach(function(container) {
  container.outerHTML = '<%= j render "follow_requests/follow_unfollow", sender: current_user, recipient: @recipient %>';
});
